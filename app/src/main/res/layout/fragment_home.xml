<ScrollView
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/colorBackground"
  >
<FrameLayout
  android:layout_width="match_parent"
  android:layout_height="match_parent">
  <RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.saikailas.ooty.organization.HomeFragment">

    <TextView
      android:id="@+id/heading"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:fontFamily="sans-serif-smallcaps"
      android:gravity="center"
      android:paddingTop="10dp"
      android:text="@string/thoughtForTheDay"
      android:textColor="#BF360C"
      android:textSize="22dp"/>

    <TextView
      android:id="@+id/thoughtitle"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/heading"
      android:fontFamily="sans-serif-smallcaps"
      android:gravity="center"
      android:paddingTop="5dp"
      android:textColor="@color/colorWhite"
      android:textSize="20dp"/>

    <ImageView
      android:id="@+id/blessingsImage"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/thoughtitle"
      android:scaleType="fitCenter"
      android:scaleX="0.80"
      android:scaleY="0.80"
      android:src="@drawable/sai_blessings"
      android:contentDescription="@string/sai_photo"/>


    <TextView
      android:id="@+id/thoughtdetail"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/blessingsImage"
      android:fontFamily="sans-serif-bold"
      android:gravity="center"
      android:textColor="@color/colorWhite"
      android:textSize="20dp"/>

  </RelativeLayout>
  <android.support.design.widget.FloatingActionButton
    android:id="@+id/fab"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom|end"
    android:layout_margin="@dimen/fab_margin"
    app:srcCompat="@drawable/ic_share_white_24dp"
    android:contentDescription="@string/share_button"/>
</FrameLayout>
</ScrollView>

